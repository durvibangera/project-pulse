# Environment Variables for P.U.L.S.E.
# Copy this file to .env and fill in values

# MLflow tracking directory (local)
MLFLOW_TRACKING_URI=./mlflow

# OpenAI (optional — only needed if not using Ollama)
# OPENAI_API_KEY=your_key_here
